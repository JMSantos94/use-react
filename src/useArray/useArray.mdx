---
name: useArray
menu: Primitives
route: /use-array
---
import { Playground } from 'docz';

import Button from 'antd/lib/button';
import List from 'antd/lib/list';

import 'antd/lib/button/style';
import 'antd/lib/list/style';

import useArray from './useArray.js';

# use-array

This primitive intends to support all (eventually) native methods of the `Array` object, with a few helper methods.

<Playground>
{() => {
  const arr = useArray([1, 2, 3]);

  const pushFive = () => arr.push(5);
  const pop = () => arr.pop();

  return (
        <>
            <Button
                onClick={pushFive}
                size="large"
                type="primary"
                style={{ margin: '.5em' }}
            >
                Push 5
            </Button>
            <Button
                onClick={pop}
                size="large"
                type="primary"
            >
                Pop
            </Button>
            <List
                style={{ margin: '.5em' }}
                bordered
                dataSource={arr.value}
                renderItem={item => (<List.Item>{item}</List.Item>)}
            />
        </>
  )
}}
</Playground>
